#!/usr/bin/env bash

set -euo pipefail

echo "Changing path to $BUILDKITE_BUILD_CHECKOUT_PATH/$BUILDKITE_PLUGIN_CHANGE_DIRECTORY_SUBDIR"
BUILDKITE_BUILD_CHECKOUT_PATH="$BUILDKITE_BUILD_CHECKOUT_PATH/$BUILDKITE_PLUGIN_CHANGE_DIRECTORY_SUBDIR"
